#include "main.h"
#include <string.h>

/**
 * _strlen - calculates the length of a string
 * @s: pointer to a char
 *
 * Return: lentgh of char
 */

int _strlen(char *s)
{
	int n;

	n = strlen(s);
	return (n);
}
