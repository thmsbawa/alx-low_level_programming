#include "main.h"

/**
 * main - tests function that prints if int if negative or positive
 * Return: Always 0 (success)
 */
int main(void)
{
	int i;

	i = 0;
	positive_or_negative(i);

	return (0);
}
